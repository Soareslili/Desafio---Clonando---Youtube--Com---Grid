* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    background: white;
}

header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
}

.logo img {
    display: block;
    height: 60px;
    fill: currentColor;
    margin: 15px;
}

.pesquisa {
    display: flex;
    justify-content: flex-end;
    border: 2px solid #cccc;
    padding: 3px;
    margin: 15px;
}

input {
    width: 300px;
    border: none;
    outline: none;
    height: 20px;
}

.nav-bar {
    display: flex;
    justify-content: space-between;
    gap: 20px;
    margin-right: 15px;
}

.nav-bar .foto-usuario {
    height: 40px;
    border-radius: 100px;
}

section {
    display: flex;
    justify-content: center;
    width: 100vw;
    height: 100vh;
}

.menu .img {
    position: fixed;
    display: inline-block;
    left: 0;
    padding: 4px;
}

.container {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-start;
    padding-top: 25px;
    margin-left: 15px;
}
